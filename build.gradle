buildscript {
  repositories {
    jcenter()
  }
  dependencies {
    classpath 'org.ajoberstar:gradle-git:0.9.0'
  }
}

apply plugin: 'github-pages'

githubPages {
  repoUri = 'git@github.com:grimrose/git-base-study-aug-2014.git'
  pages {
    from file('index.html')
  }
}
